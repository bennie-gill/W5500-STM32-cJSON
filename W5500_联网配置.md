
# W5500 联网配置与步骤（本工程适配版）

本文档基于当前工程 `i:\AI\w5500` 的实际代码与引脚配置，整理出从硬件连线、STM32 SPI 配置、W5500(ioLibrary) 适配、静态 IP 联网到 Windows 侧验证与排障的完整步骤。

## 1. 工程现状速览（你现在用的配置）

- MCU：STM32F103（HAL 工程）
- W5500 驱动：WIZnet ioLibrary（工程内 BSP 目录包含一份拷贝）
- 接口：SPI1（主机模式，8bit，Mode0）
- CS 引脚：PA3（GPIO 输出）
- 静态网络参数：IP `192.168.227.88`，掩码 `255.255.255.0`，网关 `192.168.227.162`

对应代码位置：
- SPI1 参数：[spi.c](file:///i:/AI/w5500/Core/Src/spi.c#L30-L60)
- CS 引脚与初始化：[main.h](file:///i:/AI/w5500/Core/Inc/main.h#L59-L62)、[gpio.c](file:///i:/AI/w5500/Core/Src/gpio.c#L42-L61)
- 网络参数与初始化入口：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L54-L78)
- 在 `main()` 中调用初始化：[main.c](file:///i:/AI/w5500/Core/Src/main.c#L97-L105)

## 2. 硬件连线（最小必需）

### 2.1 SPI 与 CS

本工程使用 SPI1：

| 信号 | STM32F103 引脚 | 说明 |
|---|---|---|
| SCK  | PA5 | SPI1_SCK |
| MISO | PA6 | SPI1_MISO |
| MOSI | PA7 | SPI1_MOSI |
| CS   | PA3 | 普通 GPIO，当作片选 |

来源：
- SPI1 复用管脚：[spi.c](file:///i:/AI/w5500/Core/Src/spi.c#L74-L89)
- CS 配置输出与默认拉高：[gpio.c](file:///i:/AI/w5500/Core/Src/gpio.c#L52-L61)

### 2.2 供电与时钟（容易踩坑）

- W5500 必须 3.3V 供电，且供电要稳（靠近芯片的去耦电容要齐）
- 模块需有 25MHz 时钟源（多数 W5500 模块已自带晶振/有源晶振）
- 网口变压器、RJ45 与指示灯：插上网线后至少应有 LINK 常亮（物理层最硬指标）

### 2.3 复位与中断（本工程未使用但建议确认）

- 本工程没有控制 W5500 的 RST 引脚（GPIO 未配置）。要求硬件侧保证上电复位可靠（RST 上拉 + RC、或与 MCU 复位联动）。
- INT 引脚未接入/未使用（本工程用轮询/同步 API 也能工作）。

## 3. STM32CubeMX / HAL 侧配置要点

### 3.1 SPI1 工作模式

本工程 SPI1 为：
- Master、2 线全双工、8bit
- CPOL=0、CPHA=0（SPI Mode0：`SPI_POLARITY_LOW` + `SPI_PHASE_1EDGE`）
- NSS=Soft（CS 由 PA3 手动控制）
- 分频：Prescaler=8（可后续按波形余量再调）

代码位置：[spi.c](file:///i:/AI/w5500/Core/Src/spi.c#L40-L52)

### 3.2 CS 引脚

- PA3 配置为推挽输出，默认输出高（不选中）

代码位置：[gpio.c](file:///i:/AI/w5500/Core/Src/gpio.c#L52-L61)

## 4. W5500(ioLibrary) 适配步骤（关键）

WIZnet ioLibrary 需要你把“平台相关的 SPI/CS/临界区”回调函数注册进去。否则驱动内部读写寄存器时不知道怎么操作总线。

本工程的适配在 [bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c) 内完成，流程如下：

### 4.1 实现并注册 CS 回调

- `CS_LOW()` / `CS_HIGH()` 内部用 `HAL_GPIO_WritePin()` 控制 PA3
- 通过 `reg_wizchip_cs_cbfunc(CS_LOW, CS_HIGH)` 注册给 ioLibrary

代码位置：
- CS 控制函数：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L36-L44)
- 注册回调：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L67-L78)

### 4.2 实现并注册 SPI 读写回调

本工程使用“单字节”SPI 回调：
- `wiz_spi_readbyte()`：发送 dummy `0x00` 读 1 字节
- `wiz_spi_writebyte()`：写 1 字节
- 通过 `reg_wizchip_spi_cbfunc(wiz_spi_readbyte, wiz_spi_writebyte)` 注册

底层单字节收发由 `HAL_SPI_TransmitReceive()` 完成：
- [bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L47-L53)

说明：
- ioLibrary 支持 burst 回调（一次读写多字节），但不是必需。当前实现会走“byte operation”路径，功能正确，性能足够用于基础联通与多数应用。

### 4.3 实现并注册临界区回调（可选但推荐）

ioLibrary 内部会在 SPI 事务前后调用临界区回调，避免被中断打断导致 CS/时序混乱。

本工程做法：
- `wiz_cris_enter()`：第一次进入时保存 PRIMASK 并关中断；支持嵌套计数
- `wiz_cris_exit()`：嵌套归零时按原状态恢复中断
- 通过 `reg_wizchip_cris_cbfunc(wiz_cris_enter, wiz_cris_exit)` 注册

代码位置：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L4-L25)

### 4.4 初始化芯片缓冲区（wizchip_init）

`wizchip_init(txsize, rxsize)` 用于配置 8 个 Socket 的 TX/RX 缓冲区大小（单位 KB）。

本工程配置为每个 Socket：
- TX 2KB * 8
- RX 2KB * 8

代码位置：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L67-L78)

## 5. 配置网络参数并“联网”（静态 IP）

### 5.1 网络参数结构体（wiz_NetInfo）

本工程在 `network_init()` 中写死静态参数：

- MAC：`00:08:DC:11:11:11`
- IP：`192.168.227.88`
- Mask：`255.255.255.0`
- Gateway：`192.168.227.162`
- DNS：`8.8.8.8`
- DHCP：`NETINFO_STATIC`

代码位置：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L54-L65)

### 5.2 写入 W5500 寄存器（wizchip_setnetinfo）

`wizchip_setnetinfo(&net_info)` 会将上述参数写入 W5500 的寄存器（SHAR/GAR/SUBR/SIPR 等）。

在驱动内部最终会落到（以本工程 BSP 版本为准）：
- `setSHAR()`（MAC）
- `setGAR()`（网关）
- `setSUBR()`（子网掩码）
- `setSIPR()`（本机 IP）

你可以在 [wizchip_conf.c](file:///i:/AI/w5500/BSP/wizchip_conf.c#L1379-L1390) 看到写入顺序。

### 5.3 初始化总入口（w5500_init）

本工程提供 `w5500_init()`，把“回调注册 + 芯片 init + 网络参数写入”串起来：

- 注册 `cris/cs/spi` 回调
- `wizchip_init(...)`
- `network_init()`

代码位置：[bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L67-L78)

在 `main()` 的初始化阶段调用：
- [main.c](file:///i:/AI/w5500/Core/Src/main.c#L97-L105)

## 6. Windows 侧验证步骤（建议按顺序）

### 6.1 先确认“你到底是怎么接网线的”

有两种典型拓扑，排障逻辑完全不同：

1）板子网线接路由器/交换机（推荐）
- PC 可以用 Wi‑Fi 访问同网段设备（前提：路由器没开无线隔离）

2）板子网线直连 PC 网口
- 必须让 PC 的“以太网”显示已连接，并给该网卡配置与板子同网段的静态 IP
- 如果 PC 的以太网是“媒体已断开”，那 ping 永远不通

### 6.2 用 ARP 把问题切成两段（强烈建议）

在 PC 上执行：

```bat
arp -d *
ping 192.168.227.88
arp -a
```

判读：
- `arp -a` 里看不到 `192.168.227.88`（或显示 incomplete）：连 ARP 都没通 → 优先查物理链路/网段/是否调用了初始化
- 能看到 `192.168.227.88` 对应 MAC 但 ping 超时：ARP 通、ICMP 不通 → 常见原因是 W5500 屏蔽 ping 或回包路径/网关异常

### 6.3 Wireshark 快速定位（可选但最快）

过滤条件：
- `arp`：看 PC 的 ARP Request，W5500 是否有 ARP Reply
- `icmp`：看 Echo Request/Reply 是否成对出现

## 7. 常见故障点与对策（按概率排序）

### 7.1 PC/路由器网络隔离

- 路由器开启“AP 隔离/无线客户端隔离”时：Wi‑Fi 设备访问不到有线端设备（同网段也不行）
- 直连时：PC 以太网必须真的连上（链路灯 + Windows 显示已连接）

### 7.2 SPI 不通（最常见的硬件/时序问题）

建议做“最小寄存器自检”：
- 读 W5500 `VERSIONR`，应为 `0x04`

相关宏（本工程 BSP 版本）：
- `getVERSIONR()`：[w5500.h](file:///i:/AI/w5500/BSP/w5500.h#L1633-L1634)
- `VERSIONR` 说明：[w5500.h](file:///i:/AI/w5500/BSP/w5500.h#L405-L407)

如果 `VERSIONR` 读不到 `0x04`：
- 优先用逻辑分析仪看 CS/SCK/MOSI/MISO 是否有正确时序
- 确认 SPI Mode0（本工程已是 Mode0）：[spi.c](file:///i:/AI/w5500/Core/Src/spi.c#L44-L46)

### 7.3 W5500 屏蔽 ping（Ping Block）

W5500 的 MR 寄存器有 PB 位（Ping Block）：
- `MR_PB = 0x10`

定义位置：[w5500.h](file:///i:/AI/w5500/BSP/w5500.h#L720)

ioLibrary 也提供了网络模式接口：
- `NM_PINGBLOCK` 定义在 [wizchip_conf.h](file:///i:/AI/w5500/BSP/wizchip_conf.h#L675-L682)
- `wizchip_setnetmode()` 会把 bit OR 进 MR：[wizchip_conf.c](file:///i:/AI/w5500/BSP/wizchip_conf.c#L1404-L1419)

本工程默认没有调用 `wizchip_setnetmode(NM_PINGBLOCK)`，理论上不应屏蔽 ping；但如果你后续加了相关调用或 MR 被改写，就会出现“ARP 有、ping 不通”的典型现象。

### 7.4 IP/掩码/网关不匹配

你当前配置为：
- 板子：`192.168.227.88/24`，GW=`192.168.227.162`

确保 PC 与板子位于同一二层广播域（同路由器 LAN 或直连同网段）。

## 8. 你需要改哪些参数（最常改的三处）

直接改 [bsp_w5500.c](file:///i:/AI/w5500/BSP/bsp_w5500.c#L54-L65)：
- `.ip`：板子 IP
- `.sn`：子网掩码
- `.gw`：网关（通常填路由器 LAN IP）
- `.mac`：建议保证唯一（同网段 MAC 冲突会导致极难定位的问题）

## 9. 推荐的“联通性验收清单”

- 插上网线，LINK 灯常亮
- 读 `VERSIONR == 0x04`
- `arp -a` 能看到板子 IP 对应 MAC
- `ping 192.168.227.88` 有回复（Windows 显示 TTL=xx）


